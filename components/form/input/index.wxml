<input maxlength="{{data.maxlength}}" type="{{data.inputType}}" name="{{data.prop}}" disabled="{{data.isDisabled}}"
      value="{{data.value}}" placeholder="{{data.placeholder}}" placeholder-style="{{placeholderStyle}}"
      bindinput="bindinput">
</input>